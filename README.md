# appliance_servers

Repository that stores all useful server builds, each server has its own directory with a readme file explaining the required base operating system or another build that is is built on top of
